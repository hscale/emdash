---
import { getEmDashEntry } from "emdash";
import Base from "../../layouts/Base.astro";
import MarketingBlocks from "../../components/MarketingBlocks.astro";

const { entry: page, cacheHint } = await getEmDashEntry("pages", "pricing", { locale: "zh" });

try {
	Astro.cache.set(cacheHint);
} catch {}

const pageContent = page?.data.content;
---

<Base
	title="价格方案"
	description="简单透明的价格。没有隐藏费用。没有意外。"
>
	{
		pageContent ? (
			<MarketingBlocks value={pageContent} />
		) : (
			<div class="empty-state">
				<h1>价格方案</h1>
				<p>请在管理后台添加价格内容。</p>
				<a href="/_emdash/admin" class="btn btn-primary">
					打开管理后台
				</a>
			</div>
		)
	}
</Base>

<style>
	.empty-state {
		text-align: center;
		padding: var(--spacing-5xl) var(--spacing-lg);
		max-width: var(--max-width);
		margin: 0 auto;
	}

	.empty-state h1 {
		margin-bottom: var(--spacing-md);
	}

	.empty-state p {
		color: var(--color-muted);
		margin-bottom: var(--spacing-xl);
	}
</style>
